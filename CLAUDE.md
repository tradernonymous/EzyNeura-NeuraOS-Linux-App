# neuraos-linux — notes for Claude

<!-- Kept short on purpose: every line here is read at the start of every session. -->

See `AGENTS.md` for the full rules — this file exists only because some
tooling looks for `CLAUDE.md` specifically. Everything in `AGENTS.md`
applies here unchanged.

Quick orientation: this repo ports the Tauri 2 + React desktop app from
`tradernonymous/freeopenai` (`app/desktop/`, imported and pinned via
`UPSTREAM`) to run natively on Linux Mint. The plan is `docs/MASTER_PLAN.md`;
the live phase-by-phase checklist result belongs in `docs/BACKLOG.md` once
that file exists (add it if it doesn't yet, matching upstream's own).
