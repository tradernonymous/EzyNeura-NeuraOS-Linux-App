{
  "totalBytes": 31457280,
  "largestAssetBytes": 12582912,
  "note": "Ceiling for app/desktop/dist, the frontend embedded in the binary (30 MB total, 12 MB largest asset). Set from the dependency sizes rather than a measured build; tighten it with \"node scripts/check-bundle-size.mjs --write\" once a real build is measured. Monaco and mermaid are the two large lazy chunks (they load only when a file is opened for editing, or a diagram is drawn) and together are most of this budget by design -- see app/desktop/src/components/CodeEditor.tsx and src/diagram.ts."
}
